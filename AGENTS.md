# 项目宪法

> 全部条目按触发场景组织，分三档：标注【红线】的拥有最高优先级，违反即任务失败；未标注的默认遵守，确有理由豁免时须向用户说明；标注【偏好】的为风格倾向。

## 任何时候
- 【红线】凭证不落盘、不进版本库、不回显、不硬编码；
- 【红线】不编造数据/URL/引用出处，来源不明就留空或先向用户确认；
- 【红线】不修改项目根级 AI 指令文件；
- 【红线】版本控制操作走平台接口，不拼复合命令硬闯、不碰内部元数据文件；
- 【红线】非版本控制必要内容禁止进源码目录：临时产物一律放 .temp/.cache/.local 等集中目录并加入 .gitignore。

## 收到需求时
- 需求含糊或有多种解读：先确认目标/范围/定义，不猜测实现；
- 破坏性或超出用户指向的改动：先给影响分析与可选方案，等用户拍板；用户指哪改哪；
- 把任务转化为可验证目标；多步骤任务先列计划并为每步标注验证点。

## 设计方案时
- 【红线】关键设计点（契约/字段/返回值）必须读真实代码核实，不凭猜；验证不了就标注「未验证」；
- 改动越大、决策越不可逆，越要先摸清真实范围并与用户敲定契约；
- 通用能力优先找可复用的第三方库，避免重复造轮子；引库前核实其精确语义；
- 只收敛逐字重复的逻辑；含实体特异业务逻辑的不强行泛型化。

## 写代码时
- 【红线】遵守 DRY/KISS：动手前先全局搜索可复用实现（历史代码与第三方库），必要时先重构历史代码；发现重复逻辑立即提取，收敛为单一实现，不留多处漂移副本；
- 【红线】业务代码禁止新增注释，只允许修正既有注释或在配套测试代码中写注释；清理时保留解释 why/权衡/坑的注释与 TODO/License 头；
- 每步跑门禁（依赖整理 + 构建 + 静态检查 + 测试），全绿为无回归判据；
- 测试先行：加校验/修 bug 先写会失败的测试再让其通过，测试保留防复发。

## 改已有代码时（重构/删除/迁移）
- 重构无测试覆盖或行为不明确的代码前，先立 golden-master 测试锁定现状（含既有 quirk）并跑绿；
- 替换式重构保留调用点与签名，只换内部实现；
- 删功能入口前查其后端是否与其他功能共用，共用的保留，只删独占部分；
- 批量清理注释禁止正则批删，须分批人工审阅；功能性字符串（密钥/配置/提示词/文案）绝不能删。

## 排查与修复 BUG 时
- 【红线】BUG 必须先建立配套测试复现后再修复，不能靠推理或猜测；问题反复出现时，必须引导用户提供充足证据（浏览器控制台数据、截图、日志、SQL、执行脚本等）找出根因后再修；
- 下结论前先取证（读码/跑语句/看请求/查历史），不基于假设连续改代码；
- 性能/根因结论先做最小复现或压测；被数据推翻的判断如实纠正；
- 追问根因，禁止只打增量补丁；
- 修复必须真实跑起来端到端验证并与原实现对比。

## 交付与汇报时
- 【红线】每次变更代码后，至少派两个子 agent 使用 git 对比远程代码差异，在目标一致性、编码原则、安全性、编码规范、隐含 BUG 等维度做深度 review；
- 【红线】收尾输出必须额外补充一张或多张变更前后 mermaid 图；
- 【红线】区分「我认为对」与「我验证过」：未验证的如实标注，不用「编译过」冒充「测过」；无验证证据不得说「修好了/完成」；
- 复核采用对抗式：默认存疑、尝试证伪，亲自读码确认才判成立，每条附文件行号证据；
- 子 agent 结论只是候选风险，主 agent 必须逐条实证后再采纳；
- 前端交互改动必须端到端自动化验证（含截图核对）；
- 如实汇报各层测试真实状态：跑了/skip/没做。
